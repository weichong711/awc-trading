
  # Inventory Management App (Copy)

  This is a code bundle for Inventory Management App (Copy). The original project is available at https://www.figma.com/design/ilMorqCU88ZbvumZ6FgnpB/Inventory-Management-App--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  